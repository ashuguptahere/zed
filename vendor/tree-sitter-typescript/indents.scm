; TypeScript adds its type-level bodies to JavaScript's indent query, which is
; concatenated ahead of this one (see treesitter.zig).
[
  (object_type)
  (interface_body)
  (enum_body)
] @indent.begin
